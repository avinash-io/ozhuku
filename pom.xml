<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.avinashio.ozhuku</groupId>
    <artifactId>ozhuku</artifactId>
    <version>0.1.0-SNAPSHOT</version>

    <packaging>pom</packaging>

    <name>Ozhuku</name>
    <description>
        Configuration-driven data ingestion and integration platform
    </description>

    <url>https://github.com/avinash-io/ozhuku</url>

    <modules>
        <module>foundation/ozhuku-foundation</module>
        <module>domain/ozhuku-domain</module>

        <module>ports/ozhuku-persistence-api</module>
        <module>ports/ozhuku-storage-api</module>
        <module>ports/ozhuku-format-api</module>

        <module>adapters/ozhuku-persistence-postgres</module>
        <module>adapters/ozhuku-storage-file</module>
        <module>adapters/ozhuku-format-csv</module>

        <module>application/ozhuku-application</module>

        <module>integration/ozhuku-integration-tests</module>

    </modules>

    <properties>
        <!--
            Avinash: Java 17 is the minimum supported runtime and
            compilation baseline for Ozhuku.
        -->
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Maven plugin versions are centralized here. -->
        <maven.compiler.version>3.15.0</maven.compiler.version>
        <maven.surefire.version>3.5.4</maven.surefire.version>
        <maven.resources.version>3.4.0</maven.resources.version>
        <checkstyle.version>3.6.0</checkstyle.version>
        <maven.enforcer.version>3.6.2</maven.enforcer.version>
        <maven.dependency.version>3.7.0</maven.dependency.version>
        <junit.version>6.0.1</junit.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
                Avinash: Third-party dependency versions are owned centrally
                by the root project. Add dependencies here only when Ozhuku
                actually adopts them.
            -->

            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven.enforcer.version}</version>
                    <executions>
                        <execution>
                            <id>enforce-build-environment</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <requireMavenVersion>
                                        <version>[3.9.6,)</version>
                                    </requireMavenVersion>

                                    <requireJavaVersion>
                                        <version>[17,)</version>
                                    </requireJavaVersion>

                                    <dependencyConvergence/>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven.resources.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven.dependency.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${checkstyle.version}</version>
                    <configuration>
                        <configLocation>
                            ${maven.multiModuleProjectDirectory}/config/checkstyle/checkstyle.xml
                        </configLocation>
                        <includeTestSourceDirectory>true</includeTestSourceDirectory>
                        <consoleOutput>true</consoleOutput>
                        <failsOnError>true</failsOnError>
                    </configuration>
                </plugin>

            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <executions>
                    <execution>
                        <id>checkstyle-verify</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>

    </build>

</project>